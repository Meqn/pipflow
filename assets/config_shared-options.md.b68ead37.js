import{_ as e,o,c as a,X as c}from"./chunks/framework.d24d905d.js";const g=JSON.parse('{"title":"共享配置","description":"","frontmatter":{},"headers":[],"relativePath":"config/shared-options.md","filePath":"config/shared-options.md","lastUpdated":1692841988000}'),s={name:"config/shared-options.md"},d=c('<h1 id="shared-options" tabindex="-1">共享配置 <a class="header-anchor" href="#shared-options" aria-label="Permalink to &quot;共享配置 {#shared-options}&quot;">​</a></h1><h2 id="base" tabindex="-1">base <a class="header-anchor" href="#base" aria-label="Permalink to &quot;base {#base}&quot;">​</a></h2><ul><li><strong>类型：</strong> <code>string</code></li><li><strong>默认：</strong> <code>./src</code></li></ul><p>显式地在创建的 Vinyl 对象上设置 base 属性。</p><p><code>glob base</code> (有时称为 glob parent)是 <code>glob</code> 字符串中任何特殊字符之前的路径段。因此，<code>/src/js/**.js</code> 的 blob base 是 <code>/src/js/</code>。所有匹配 <code>glob</code> 的路径都保证共享 <code>glob base</code>，该路径段不能是可变的。</p><p>由 <code>src()</code> 生成的 Vinyl 实例是用 glob base 集作为它们的 base 属性构造的。当使用 <code>dest()</code> 写入文件系统时，将从输出路径中删除 base ，以保留目录结构。</p><p>有关更深入的信息，请参阅 <a href="https://github.com/es128/glob-parent" target="_blank" rel="noreferrer">glob-parent</a> 库。</p><h2 id="public-dir" tabindex="-1">publicDir <a class="header-anchor" href="#public-dir" aria-label="Permalink to &quot;publicDir {#public-dir}&quot;">​</a></h2><ul><li><strong>类型：</strong> <code>string | false</code></li><li><strong>默认：</strong> <code>&quot;public&quot;</code></li></ul><p>作为静态资源服务的文件夹。该目录中的文件在开发期间在 <code>/</code> 处提供，并在构建期间复制到 <code>outDir</code> 的根目录，并且始终按原样提供或复制而无需进行转换。该值可以是文件系统的绝对路径，也可以是相对于项目根目录的相对路径。</p><p>将 <code>publicDir</code> 设定为 <code>false</code> 可以关闭此项功能。</p><h2 id="alias" tabindex="-1">alias <a class="header-anchor" href="#alias" aria-label="Permalink to &quot;alias {#alias}&quot;">​</a></h2><ul><li><strong>类型：</strong> <code>{ [key string]: string }</code></li></ul><p>全局字符串别名，在处理<code>html</code>, <code>javascript</code>, <code>css</code> 文件时, 每一项将会被静态替换。</p><div class="warning custom-block"><p class="custom-block-title">友情提醒</p><p>这里的 <code>alias</code> 仅仅是字符串的别名替换，不同于 <code>webpack</code>, <code>vite</code> 等 <code>alias</code> 功能。</p></div><h2 id="env-dir" tabindex="-1">envDir <a class="header-anchor" href="#env-dir" aria-label="Permalink to &quot;envDir {#env-dir}&quot;">​</a></h2><ul><li><strong>类型：</strong> <code>string</code></li><li><strong>默认：</strong> <code>root</code></li></ul><p>用于加载 <code>.env</code> 文件的目录。可以是一个绝对路径，也可以是相对于项目根的路径。</p><p>关于环境文件的更多信息，请参见 <a href="./../guide/env-and-mode.html">这里</a>。</p>',19),t=[d];function r(i,l,n,p,b,h){return o(),a("div",null,t)}const _=e(s,[["render",r]]);export{g as __pageData,_ as default};
