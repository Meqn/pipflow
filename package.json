{
  "name": "pip",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clear": "rm -rf node_modules **/*/node_modules dist **/*/dist",
    "flow": "pnpm -F demo-flow run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish --registry https://registry.npmjs.org"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "gulp": "^4.0.2",
    "rimraf": "5.0.1"
  }
}
